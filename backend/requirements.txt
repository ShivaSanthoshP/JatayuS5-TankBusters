# FastAPI & server
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
python-dotenv==1.0.1
pydantic==2.10.4
eval_type_backport==0.3.1

# Database
sqlalchemy==2.0.36
aiosqlite==0.20.0
# PostgreSQL driver — used in production (Mumbai EC2) when DATABASE_URL is
# a postgresql:// URL. The binary wheel avoids needing libpq-dev at build time.
psycopg2-binary==2.9.10

# LangChain / LangGraph / LLM
langchain==0.3.14
langchain-ollama==0.2.2
langchain-core==0.3.29
langgraph==0.2.60
ollama>=0.5.0

# Cloud LLM providers (optional — only used if selected in Settings)
openai>=1.40.0
google-genai>=0.3.0

# Vector DB
chromadb>=0.5.0
numpy>=1.26.0

# WebSocket
websockets==14.1

# Cloud provider adapters
boto3>=1.34.0
azure-identity>=1.15.0
azure-monitor-query>=1.3.0
azure-mgmt-compute>=30.0.0
azure-mgmt-resource>=23.0.0
google-cloud-monitoring>=2.18.0
google-auth>=2.28.0
